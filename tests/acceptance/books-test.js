import { test } from 'qunit';
import moduleForAcceptance from 'book-share/tests/helpers/module-for-acceptance';

moduleForAcceptance('Acceptance | books');

/* test('visiting /book', function(assert) {
  visit('/book/1');
  var c = getController('book');
  var a = c.get('isAvailable');
  console.log('@@@', a);

  andThen(function() {
    assert.equal(currentURL(), '/book/1','route test is done');
  });
}); */
