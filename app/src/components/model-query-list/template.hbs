{{#if queryItems.isRunning}}
    {{#if loadingComponent}}
        {{component loadingComponent}}
    {{else}}
        Loading...
    {{/if}}
{{else}}
    {{#each items as |item|}}
       {{yield item}}
    {{else}}
    {{yield}}
    {{/each}}
{{/if}}